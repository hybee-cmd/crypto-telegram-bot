services:
  - type: worker
    name: crypto-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python advanced_crypto_bot.py
    envVars:
      - key: TOKEN
        value: your_actual_bot_token_here
